import { dateSubmitted } from '../../../helpers/date-submitted';
import { module, test } from 'qunit';

module('Unit | Helper | date submitted');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = dateSubmitted(42);
  assert.ok(result);
});
